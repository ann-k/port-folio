<%= form_with(model: project_in_portfolio, local: true) do |form| %>
  <% if project_in_portfolio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_in_portfolio.errors.count, "error") %> prohibited this project_in_portfolio from being saved:</h2>

      <ul>
        <% project_in_portfolio.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :position %>
    <%= form.number_field :position %>
  </div>

  <div class="field">
    <%= form.label :portfolio_id %>
    <%= form.number_field :portfolio_id %>
  </div>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.number_field :project_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
