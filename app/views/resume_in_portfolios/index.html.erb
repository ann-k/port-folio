<p id="notice"><%= notice %></p>

<h1>Resume In Portfolios</h1>

<table>
  <thead>
    <tr>
      <th>Order</th>
      <th>Portfolio</th>
      <th>Resume</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @resume_in_portfolios.each do |resume_in_portfolio| %>
      <tr>
        <td><%= resume_in_portfolio.order %></td>
        <td><%= resume_in_portfolio.portfolio_id %></td>
        <td><%= resume_in_portfolio.resume_id %></td>
        <td><%= link_to 'Show', resume_in_portfolio %></td>
        <td><%= link_to 'Edit', edit_resume_in_portfolio_path(resume_in_portfolio) %></td>
        <td><%= link_to 'Destroy', resume_in_portfolio, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Resume In Portfolio', new_resume_in_portfolio_path %>
